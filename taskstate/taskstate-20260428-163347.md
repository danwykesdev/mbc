### 🎯 Project Context
- **Project/Feature:** MBC modular Webflow runtime with Barba, Webflow reinit, GSAP/ScrollTrigger, Lenis, Finsweet, Vimeo.
- **Current Goal:** Stabilize Projects route-entry behavior so filters, pagination, and horizontal scroll reinitialize correctly when navigating to Projects from other pages.
- **Stack/Engine:** Webflow SPA runtime, Barba.js, GSAP/ScrollTrigger, Lenis, Finsweet Attributes v2, Vimeo.

### 🏗️ Current State
- **Completed:**
  - Projects route-enter hardening is implemented, committed, pushed, and rebuilt.
  - Projects now restores main-instance wiring for `fs-list-element="filters"` and `fs-list-element="scroll-anchor"` before Finsweet init.
  - Projects restart calls are queued until the list module is confirmed ready.
  - Horizontal scroll gets a deterministic post-bind reflow checkpoint after list-driven DOM updates.
  - Docs and task log were updated to reflect the current runtime contracts.
- **Active Files/Scripts:**
  - `pages/projects.js`
  - `features/finsweet.js`
  - `features/horizontal-scroll.js`
  - `docs/features/finsweet.md`
  - `docs/pages/projects.md`
  - `task.md`
  - `dist/mbc.runtime.js`

### ⚙️ Architecture & Constraints
- **Key Decisions:**
  - Keep Projects-specific routing logic in `pages/projects.js`.
  - Serialize Finsweet operations rather than allowing overlapping init/restart/destroy calls.
  - Use a route-enter readiness guard before allowing Projects restarts.
  - Reflow horizontal scroll after list updates instead of tearing it down on every bind.
- **Environment/Rules:**
  - Production testing uses the rebuilt `dist/mbc.runtime.js` via jsDelivr hash-pinned URL.
  - Do not rely on legacy runtime files for live behavior.
  - Update `task.md` whenever a meaningful change lands.

### ⏭️ Next Steps
- **Pending Tasks:**
  - Test the latest pushed hash in Webflow / CDN.
  - Verify Home -> Projects, other page -> Projects, and Projects -> detail -> Projects flows.
  - Confirm filters, pagination, and horizontal scroll all initialize on route entry.
- **Known Blockers:**
  - No active code blocker; the remaining work is live verification in the browser.
