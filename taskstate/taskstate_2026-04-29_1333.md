### 🎯 Project Context
- **Project/Feature:** MBC Modular Webflow JS Runtime
- **Current Goal:** Stabilize SPA page transitions (Barba.js), particularly on the Projects page involving Horizontal Scroll, Finsweet Filters, and Pagination.
- **Stack/Engine:** Webflow, JS (ESBuild), Barba.js, GSAP ScrollTrigger, Finsweet V2

### 🏗️ Current State
- **Completed:** 
  - Fixed filter tab scroll jump by removing redundant list restart bindings that forced `ScrollTrigger.refresh(true)` on tab switches.
  - Fixed horizontal scroll SPA unreachable trigger (start pos ~5380px) by delaying the `ScrollTrigger.refresh` by 500ms to allow Barba to finish container removal.
  - Restored Finsweet filter attribute scrubbing to prevent automatic scroll-anchor behavior.
  - Resolved the Projects SPA pagination reload bug by making fresh Finsweet init load-only and leaving restart() for runtime refresh paths.
- **Active Files/Scripts:** 
  - `features/finsweet.js`
  - `pages/projects.js`
  - `features/horizontal-scroll.js`

### ⚙️ Architecture & Constraints
- **Key Decisions:** 
  - Standardized on delayed `ScrollTrigger.refresh` post-SPA navigation to ensure accurate DOM position measurements.
  - Separated Finsweet's virtual DOM concerns from native Webflow interactions (e.g. stripping `fs-list-element` from tab wrappers).
- **Environment/Rules:** 
  - **State Testing Matrix:** 
    - Hard Load (Direct URL load)
    - Reload (Barba transition: other page > current page)
  - **Feature Status:**
    - Horizontal Scroll: works load, works reload
    - Filters: works load, works reload
    - Pagination: works load, code fix applied for reload path; browser verification next

### ⏭️ Next Steps
- **Pending Tasks:** 
  - Re-test the Projects reload path in the browser and confirm the init-safe Finsweet load path renders the full list.
- **Known Blockers:** 
  - No code blocker remains; the remaining work is live browser verification.
