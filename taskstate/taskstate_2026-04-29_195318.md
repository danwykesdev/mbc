### 🎯 Project Context
- **Project/Feature:** Webflow/Barba front-end with a Projects page using Finsweet Attributes V2 for CMS list loading, filtering, and horizontal scrolling.
- **Current Goal:** Fix SPA transitions into the Projects page so `fs-list-load="all"` loads the full item set, and reduce debug snapshot noise.
- **Stack/Engine:** Webflow, Barba SPA routing, Finsweet Attributes V2, vanilla JS.

### 🏗️ Current State
- **Completed:** Updated `features/finsweet.js` to add a pre-load settle wait when reinitializing list modules after destroy; trimmed debug snapshot payload in `core/utils.js`; build passes.
- **Active Files/Scripts:** `features/finsweet.js`, `core/utils.js`, `pages/projects.js`, `plan/feature/projects/plan.md`.

### ⚙️ Architecture & Constraints
- **Key Decisions:** Keep `restart()` behavior for live filter/tab refreshes, but avoid render-only restarts during fresh Projects init; allow Finsweet time to clear stale list state before `fs.load()` on SPA entry.
- **Environment/Rules:** Route debug data should be compact; hard-load behavior must not regress; preserve Finsweet async load lifecycle and avoid disrupting `fs-list-load="all"` fetch chains.

### ⏭️ Next Steps
- **Pending Tasks:** Validate SPA transitions `home → projects`, `zine → projects`, and `about → projects` now render all CMS items; collect new debug logs if issues remain; adjust if stale Finsweet state still leaks.
- **Known Blockers:** None active after the latest fix; the prior failure mode was stale `fs.modules.list` and racing list load state on SPA reload.
